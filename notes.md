Models.py

int | None for the primary key field is used so that in Python code we can create an object without an id (id=None), assuming the database will generate it when saving.

CYCLE detection with BFS, because family trees  are usually wider than deeper.


AI used: help generate tests.

Generate cycle search based on bfs